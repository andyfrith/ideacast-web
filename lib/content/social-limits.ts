/** X post hard limit for classic tweets. */
export const TWITTER_MAX_CHARS = 280;

/** Soft guidance for LinkedIn feed posts (not enforced by the API). */
export const LINKEDIN_SOFT_MAX_CHARS = 3000;
